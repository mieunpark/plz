import moment from 'moment';
import React from 'react';

import MainStackPresenter from './MainStackPresenter';

const MainStackContainer = ({navigation}) => {

  return (
    <MainStackPresenter />
  );
};

export default MainStackContainer;
