import moment from 'moment';
import React from 'react';

import CheckPresenter from './CheckPresenter';


const CheckContainer = ({navigation}) => {

  return (
    <CheckPresenter />
  );
};

export default CheckContainer;
